pre-commit:
  parallel: false
  commands:
    format:
      glob: '*.{java}'
      run: ./devops/format.sh {staged_files} && git add {staged_files}
    test:
      glob: '*.{java}'
      run: ./devops/test.sh

# docker buildx build \
#   --platform linux/amd64,linux/arm64 \
#   --tag henrotaym/spring-boot-games-http:latest \
#   --target http \
#   --push \
#   .
